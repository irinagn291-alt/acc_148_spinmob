import Foundation

/// Curated, SFW emoji set offered for "custom stickers on segments" — a fixed
/// palette keeps segment labels predictable instead of accepting arbitrary text.
enum StickerCatalog {
    static let all: [String] = [
        "🎉", "🔥", "💀", "👑", "🍕", "🍻", "😂", "💋",
        "🎲", "⭐️", "🤡", "🥳", "🤫", "💃", "🕺", "🚨"
    ]
}
